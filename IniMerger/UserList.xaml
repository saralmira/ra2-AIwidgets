<UserControl x:Class="IniMerger.UserList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IniMerger"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100">
    <Border BorderThickness="2" Grid.Row="0" BorderBrush="AliceBlue">
        <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
            <ScrollViewer.Content>
                <StackPanel x:Name="src_list"  Orientation="Vertical" CanVerticallyScroll="True" ToolTip="{StaticResource tip_ul}"
                            Background="#00FFFFFF" ScrollViewer.VerticalScrollBarVisibility="Auto">

                </StackPanel>
            </ScrollViewer.Content>
        </ScrollViewer>
    </Border>
</UserControl>
